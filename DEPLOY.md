# Railway: собрать всё в проект `nanocem.app` — сценарий для Claude в Chrome

Задача: **новый** проект Railway с именем `nanocem.app`, внутри — все наши приложения и их базы,
домен и ключи на месте. Проект `easymix` — другой продукт, его не трогаем.
Работа идёт в группе вкладок; скопируйте промпт целиком в Claude в браузере.

## Итоговая схема

| Сервис в проекте `nanocem.app` | Источник | Что отдаёт | Адрес |
|---|---|---|---|
| `nanocem-app` | `qazaqboss/well-343-karatube`, Dockerfile в корне | сайт аналитики + AI-помощник `/ai` + ERP `/erp/` | `nanocem.app` |
| `subsoil-management` | `qazaqboss/subsoil-management`, Dockerfile в корне (собирает React-фронт и FastAPI разом) | контроль лицензионных обязательств | `erp.nanocem.app` |
| `postgres-management` | Railway Postgres | база для subsoil-management — **обязательна** | внутренний |
| `postgres-nanocem` | Railway Postgres | про запас для nanocem-app — **код её пока не использует** | внутренний |

## Вкладки в группе (открыть заранее)

| № | Вкладка | Зачем |
|---|---|---|
| 1 | https://railway.com/dashboard | проекты, сервисы, переменные, базы |
| 2 | https://nanocem.app/ | проверка основного сервиса |
| 3 | https://dash.cloudflare.com/ | DNS домена (nameservers уже переведены сюда) |
| 4 | https://github.com/qazaqboss/subsoil-management | справка по второму приложению |
| 5 | https://github.com/qazaqboss/well-343-karatube | справка по основному репозиторию |

---

## ПРОМПТ (копировать целиком)

```
Ты работаешь в моей группе вкладок в Chrome. Задача — собрать все мои приложения в ОДИН новый
проект Railway с именем nanocem.app, завести каждому базу Postgres, перенести API-ключ и домен,
и проверить, что всё отвечает.

Правила работы:
— Иди строго по шагам, не забегай вперёд.
— Там, где написано «СТОП», остановись, напиши результат и жди моего ответа.
— Ничего не удаляй, пока я не подтвердил удаление в этом конкретном шаге.
— НИКОГДА не выписывай мне значения секретов (API-ключи, пароли, DATABASE_URL, SECRET_KEY).
  Секреты переноси только копированием между вкладками через Raw Editor. В переписке пиши
  максимум «ключ скопирован», «переменная задана».
— В проекте easymix и в любом другом, который я не называл, НИЧЕГО не меняй, не переименовывай
  и не удаляй. Это другие продукты. Оттуда мы только копируем переменные и в самом конце удаляем
  те сервисы, которые я прямо назову.
— Если название кнопки отличается от указанного, найди ближайшую по смыслу. Не уверен — спроси.

═══ ЧАСТЬ 1. Инвентаризация ═══

1. Вкладка 1. Выпиши ВСЕ проекты аккаунта, и по каждому сервису:
   имя сервиса · источник (репозиторий и ветка либо образ) · публичный домен ·
   есть ли Volume и точка монтирования · ИМЕНА переменных (без значений) · есть ли базы данных.

   СТОП. Пришли таблицей и жди ответа: я скажу, что переносим, а что оставляем на месте.

═══ ЧАСТЬ 2. Новый проект и ключ ═══

2. Создай НОВЫЙ проект: дашборд → New Project → Empty Project. Назови его nanocem.app.
   Существующие проекты не переименовывай.

3. Найди сервис с доменом subsoil-ai-production.up.railway.app — он сейчас отдаёт сайт nanocem.app,
   и в нём настроен ключ Anthropic. Запомни, в каком проекте он лежит.

4. Сохрани ключ: этот сервис → Variables → Raw Editor → скопируй строку ANTHROPIC_API_KEY целиком.
   Значение мне не показывай. Заодно выпиши ИМЕНА остальных переменных этого сервиса.

   СТОП. Напиши, в каком проекте нашёлся сервис, его переменные по именам и «ключ скопирован».

═══ ЧАСТЬ 3. Основной сервис в новом проекте ═══

5. Проверь, есть ли в Railway штатный перенос сервиса между проектами: сервис из шага 3 →
   Settings → поищи пункт вида «Move to project» / «Transfer service».
   СТОП. Напиши, есть такой пункт или нет.

6. ЕСЛИ перенос есть — перенеси сервис в проект nanocem.app и переименуй в nanocem-app.
   ЕСЛИ переноса нет — создай заново: проект nanocem.app → + New → GitHub Repo →
   qazaqboss/well-343-karatube, ветка main, имя сервиса nanocem-app.
   Настройки сборки не трогай, в репозитории есть Dockerfile.

7. Variables сервиса nanocem-app: ANTHROPIC_API_KEY (ключ из шага 4) и DB_PATH = /data/erp.db.

8. Settings → Volumes: том с точкой монтирования /data. Если тома нет — создай.
   Данные со старого тома не переезжают; для ERP не страшно — стартовая база подтянется
   из репозитория автоматически.

9. Задеплой, дождись Success. Упало — СТОП, пришли последние 30 строк лога.

10. Networking → Generate Domain. Открой служебный адрес НОВОГО сервиса и проверь пять страниц:
    / · /stages.html · /ai · /erp/ · /erp/reports/stage/1

    СТОП. Дальше идём только если все пять работают на новом сервисе.

═══ ЧАСТЬ 4. Базы данных ═══

11. В проекте nanocem.app: + New → Database → Add PostgreSQL. Переименуй базу в postgres-management.

12. Ещё раз: + New → Database → Add PostgreSQL. Переименуй в postgres-nanocem.
    Эта база пока про запас: код nanocem-app её не использует, он работает с SQLite на томе.
    Если я скажу «не нужна» — пропусти шаг, платить за простаивающую базу смысла нет.

    СТОП. Напиши, какие базы созданы.

═══ ЧАСТЬ 5. Второе приложение — subsoil-management ═══

13. В проекте nanocem.app: + New → GitHub Repo → qazaqboss/subsoil-management, ветка main.
    Имя сервиса subsoil-management. Root Directory оставь пустым — в корне лежит Dockerfile,
    который собирает React-фронтенд и FastAPI в один образ, отдельный сервис под фронт не нужен.

14. Variables сервиса subsoil-management:
    — DATABASE_URL — ссылка на базу: ${{postgres-management.DATABASE_URL}}
      (подстановка переменной другого сервиса, набирается со знаком ${{ ).
    — SECRET_KEY — сгенерируй случайную строку из 64 шестнадцатеричных символов.
      Не словарное слово, не «password», ключ Anthropic сюда не подставляй. Мне не показывай.
    — ANTHROPIC_API_KEY — ключ из шага 4.

15. Settings → Healthcheck Path = /api/health. Задеплой, дождись Success.
    Упало — СТОП, последние 30 строк лога: скорее всего дело в подключении к базе.

16. Networking → Generate Domain. Проверь: главная открывается, /api/health отвечает.

    СТОП. Напиши, что открылось.

═══ ЧАСТЬ 6. Домены ═══

17. Домен nanocem.app сейчас висит на СТАРОМ сервисе, его надо перевесить на новый.
    Домен может принадлежать только одному сервису, поэтому на пару минут сайт будет доступен
    только по служебному адресу — это ожидаемо.
    — Старый сервис → Networking → удали кастомный домен nanocem.app.
    — Сервис nanocem-app → Networking → Custom Domain → nanocem.app. Railway покажет CNAME-таргет.
    — Вкладка 3 (Cloudflare, зона nanocem.app) → DNS: обнови запись для корня на новый таргет.
      Proxy status — DNS only, серое облако.
    — Дождись зелёной отметки в Railway и открой https://nanocem.app.

    СТОП. Напиши, что сайт открылся на новом сервисе.

18. Поддомен для второго приложения:
    — Сервис subsoil-management → Networking → Custom Domain → erp.nanocem.app.
    — Cloudflare → DNS → Add record: CNAME, имя erp, значение — таргет из Railway,
      Proxy status — DNS only.
    — Дождись подтверждения и открой https://erp.nanocem.app.

    СТОП. Напиши итог.

═══ ЧАСТЬ 7. Уборка ═══

19. Старый сервис, с которого сняли домен (тот, что был subsoil-ai-production): покажи мне его
    имя, проект и источник. СТОП, дождись подтверждения — и только потом удали.

20. Сервис с доменом well-343-karatube-production.up.railway.app — устаревший дубль сайта.
    Покажи имя и проект. СТОП, дождись подтверждения — и только потом удали.

21. Если после этого какой-то проект остался совсем пустым и он НЕ easymix и не из тех, что я
    просил не трогать — покажи мне его имя. СТОП, дождись подтверждения — и только потом удали.

22. Итоговый отчёт: что теперь в проекте nanocem.app — сервисы, базы, домены и имена переменных
    у каждого сервиса. И отдельно: что осталось в других проектах нетронутым.

═══ ОТКАТ ═══

Удалённый сервис не восстанавливается, но пересоздаётся из того же репозитория за пару минут —
теряются только переменные и данные тома. Поэтому переменные копируем ДО удаления, а удаляем
только после того, как новый сервис отвечает.

Домен: если после перевода записи сайт не открывается — верни в Cloudflare прежний CNAME-таргет
(старого сервиса), домен вернётся на старое место.
```

---

## Справочные данные

| Что | Значение |
|---|---|
| Целевой проект | `nanocem.app` — **новый**, создаётся с нуля |
| Основной репозиторий | `qazaqboss/well-343-karatube`, ветка `main` |
| Второе приложение | `qazaqboss/subsoil-management`, ветка `main`, Root Directory — корень |
| Переменные `nanocem-app` | `ANTHROPIC_API_KEY`, `DB_PATH=/data/erp.db` |
| Переменные `subsoil-management` | `DATABASE_URL`, `SECRET_KEY`, `ANTHROPIC_API_KEY` |
| Том | `/data` у `nanocem-app` |
| Домены | `nanocem.app` → `nanocem-app`, `erp.nanocem.app` → `subsoil-management` |
| DNS | Cloudflare (nameservers переведены), регистрация домена — Vercel |

## Что важно знать

- **Postgres для `nanocem-app` создаётся на вырост.** Код ERP сейчас работает с SQLite на томе
  `/data`. Пока не переписан слой доступа к данным, база будет простаивать и просто стоить денег.
  Перевод ERP на Postgres — отдельная задача по коду, не для браузерного агента.
- **`subsoil-management` без Postgres не поднимется** — там SQLAlchemy и psycopg2, приложение
  ждёт `DATABASE_URL` при старте.
- **Тома между проектами и сервисами не переносятся.** Новый том создаётся пустым; для ERP это не
  страшно — стартовая база копируется из репозитория автоматически, но данные, введённые в ERP
  после деплоя, живут только на своём томе.
- **Домен привязывается к одному сервису.** Поэтому второе приложение получает поддомен.
- **Удаление проекта необратимо** — уносит сервисы, тома и историю деплоев.
- **Проект `easymix` не трогаем** — это другой продукт. Все наши сервисы переезжают
  в новый проект `nanocem.app`, чужие проекты остаются как есть.
